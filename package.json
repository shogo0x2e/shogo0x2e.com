{
  "name": "shogo0x2e-portfolio",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev --host 127.0.0.1",
    "build": "astro build",
    "preview": "astro preview",
    "test:locale": "npm run build && node scripts/check-locale-redirect.mjs"
  },
  "dependencies": {
    "astro": "^7.0.0"
  }
}
