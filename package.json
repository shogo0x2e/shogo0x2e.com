{
  "name": "shogo0x2e-portfolio",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "generate:metadata": "node scripts/generate-build-metadata.mjs",
    "predev": "npm run generate:metadata",
    "dev": "astro dev --host 127.0.0.1",
    "prebuild": "npm run generate:metadata",
    "build": "astro build",
    "preview": "astro preview",
    "test:locale": "npm run build && node scripts/check-locale-redirect.mjs"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^7.0.0"
  }
}
